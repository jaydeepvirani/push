package com.android.unideal.data;

/**
 * This would help for identify the message type
 * when the new notification receive inside the
 * application
 */
public enum NotificationStatus {
  NEW_JOB_POST, REQUEST_ACCEPTS, PAYMENT_CLEAR, PAYMENT_TRANSFER, JOB_PAUSED
}
